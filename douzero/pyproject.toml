[project]
name = "douzero-inference"
version = "0.1.0"
description = "DouZero ONNX inference service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "onnxruntime",
]
